import chalk from 'chalk';

export async function initCommand(options: { name?: string }) {
  console.log(chalk.yellow('⚡ zapit init — coming soon'));
  console.log('This will scaffold a full NestJS + DynamoDB + CDK project.');
}
